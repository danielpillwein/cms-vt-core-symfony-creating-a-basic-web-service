{% extends 'base.html.twig' %}

{% block title %}TimeMachineEntry{% endblock %}

{% block body %}
    <h1>TimeMachineEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ time_machine_entry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ time_machine_entry.name }}</td>
            </tr>
            <tr>
                <th>ResourceURL</th>
                <td>{{ time_machine_entry.resourceURL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_time_machine_entry_index') }}">back to list</a>

    <a href="{{ path('app_time_machine_entry_edit', {'id': time_machine_entry.id}) }}">edit</a>

    <a href="{{ path('app_time_machine_entry_delete', {'id': time_machine_entry.id}) }}">delete</a>

    {{ include('time_machine_entry/_delete_form.html.twig') }}
{% endblock %}
